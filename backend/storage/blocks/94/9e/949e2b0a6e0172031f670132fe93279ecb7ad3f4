ZF：记录指令执行后数值=0-->ZF=1
PF:                          1的个数奇偶数=偶-->PF=1
SF：                                 为负数-->SF=1
CF:                                   借位值/进位-->CF

cmp 233
jnz跳转条件：ZF=0 即结果不为0就跳转
jz                    ZF=1
xor：相同为0，不同为1
test：按位与：都为1才为1

1. jne (Jump if Not Equal)

跳转条件： 如果 ZF = 0，即 零标志位没有被设置（表示两个操作数不相等），则跳转。

对应的操作： jne 会在比较操作后使用 ZF 标志来判断是否跳转。cmp 或 test 等指令会设置 ZF 标志。比如：

如果 cmp eax, ebx，当 eax != ebx 时，ZF 被清除（ZF = 0），则 jne 会跳转。

如果 eax == ebx，则 ZF = 1，jne 不会跳转。

2. je (Jump if Equal)

跳转条件： 如果 ZF = 1（表示两个操作数相等），则跳转。

对应的操作： je 是 jne 的反操作，它依赖于 ZF 标志。当 cmp 指令比较两个值并发现它们相等时，ZF 会被设置为 1，je 会跳转。

3. jg (Jump if Greater)

跳转条件： 如果 ZF = 0 且 SF = OF（即结果是正数且没有符号溢出），则跳转。

对应的操作： 这是针对 有符号数 的跳转，表示两个操作数相比较后，第一个操作数大于第二个操作数。ZF = 0 保证两者不相等，SF = OF 确保结果为正（即没有符号溢出，表示大于）。

4. jl (Jump if Less)

跳转条件： 如果 SF ≠ OF（符号标志和溢出标志不同），则跳转。

对应的操作： 这是针对 有符号数 的跳转，表示第一个操作数小于第二个操作数。当符号标志和溢出标志不一致时，说明比较结果为负数。

5. jge (Jump if Greater or Equal)

跳转条件： 如果 SF = OF，则跳转。

对应的操作： 这是针对 有符号数 的跳转，表示第一个操作数大于或等于第二个操作数。当符号标志和溢出标志一致时，说明比较结果为正数或者相等。

6. jle (Jump if Less or Equal)

跳转条件： 如果 ZF = 1 或 SF ≠ OF，则跳转。

对应的操作： 这是针对 有符号数 的跳转，表示第一个操作数小于或等于第二个操作数。当值相等（ZF = 1）或者第一个值小于第二个值（SF ≠ OF）时，jle 会跳转。

7. jc (Jump if Carry)

跳转条件： 如果 CF = 1（即进位标志被设置），则跳转。

对应的操作： 这是针对 无符号数 的跳转，表示加法或比较时发生了进位。

8. jnc (Jump if No Carry)

跳转条件： 如果 CF = 0，即没有进位，则跳转。



![image-20260320092017611](.assets/image-20260320092017611.png)
